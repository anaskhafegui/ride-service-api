// trip-status.enum.ts
export enum TripStatusEnum {
  PENDING_ASSIGNMENT = 'pending_assignment',
  ACCEPTED = 'accepted',
  ON_WAY = 'on_way',
  ARRIVED = 'arrived',
  STARTED = 'started',
  ENDED = 'ended',
}
